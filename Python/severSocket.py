
# Socket are more like the nodes for eg interned nodes p2p network
# client server connection Base of the network this is called the socket
# port number and Type of connections
# public address Lan that has the private ip address
# server it should have its must be avaible
# TCP : connection oriented protocol ( connection requiredand UDP : no ack if received pkg connection less protocol


